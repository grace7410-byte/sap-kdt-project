# 개발일지 (2026-08-15)

## 어제까지 진행 상황
- 00-공통 서치헬프: 여전히 3/12(개) 완료
- 01-자재: Determination은 점검만 하면 되는 수준, Validation 마무리하면 CRUD 끝. Value Help는 1개(자재) 추가만 남음
- 02-FI: 오늘(8/15)부터 시작해서 8/17(월)까지 마쳐야 함

## 오늘 목표
- Validation 로직 마무리
- 남은 서치헬프 작업
- 서치헬프를 MARA 프로그램에 전부 연결
- FI 보고서 착수 (가장 중요)

## 진행 내용

오늘은 실질적인 진도는 거의 못 나갔다. 어제 내용 정리 + 오늘 계획을 세우는 데 시간을 많이 썼고, 시간이 부족해서 계획을 한 번 수정했다.

> 계획 수정: 서치헬프 마무리 → FI 시작 → 화면 띄우고 MM 수정 (원래는 Validation부터 마무리하려 했으나 순서 변경)

### [00] F4 Help — 플랜트/저장위치 점검
다시 보니 각 서치헬프에 서치 필드가 이미 있었는데 MARA 쪽엔 주석 처리된 채로 방치되어 있었음 — 주석만 해제해서 연결.

[`ZI_B07_WERKS_F4`](../../reference/00_common-searchhelp.md) · [코드 보기](https://github.com/grace7410-byte/sap-kdt-project/blob/main/src/00_common-searchhelp/cds/ZI_B07_WERKS_F4.ddls.asddls)
```
@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: '플랜트 F4 Help'
@Metadata.ignorePropagatedAnnotations: true
//@ObjectModel.resultSet.sizeCategory: #XS
@Search.searchable: true
define view entity ZI_B07_WERKS_F4
  as select from t001w as w
    inner join   t001k as k on w.bwkey = k.bwkey
{
      @EndUserText.label: 'Plant'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 10 }]
  key w.werks as Plant,
      @EndUserText.label: 'Plant Name'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 20 }]
      w.name1 as PlantName,
      @EndUserText.label: 'Company Code'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 30 }]
      k.bukrs as CompanyCode
}
where
  k.bukrs = 'K200'
```

[`ZI_B07_LGORT_F4`](../../reference/00_common-searchhelp.md) · [코드 보기](https://github.com/grace7410-byte/sap-kdt-project/blob/main/src/00_common-searchhelp/cds/ZI_B07_LGORT_F4.ddls.asddls)
```
@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: '저장위치 Search Help'
@Metadata.ignorePropagatedAnnotations: true
//@ObjectModel.resultSet.sizeCategory: #XS
@Search.searchable: true
define view entity ZI_B07_LGORT_F4
  as select from I_StorageLocation
{
      @EndUserText.label: 'Plant'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 10 }]
  key Plant,
      @EndUserText.label: 'Storage Location'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 20 }]
  key StorageLocation,
      @EndUserText.label: 'Plant Name'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 30 }]
      _Plant.PlantName,
      @EndUserText.label: 'Description'
      @Search.defaultSearchElement: true
      @UI.selectionField: [{ position: 40 }]
      StorageLocationName
}
where
  _Plant._ValuationArea.CompanyCode = 'K200'
```

## 오늘 배운 점 / 막힌 점
- 서치헬프를 만들어놓고도 실제 화면(MARA)에 연결하는 걸 깜빡하기 쉽다는 걸 깨달음 — 앞으로는 CDS 작성 후 바로 연결까지 확인하는 습관이 필요할 것 같다
- 계획한 작업 순서를 상황(시간 부족)에 맞게 유연하게 조정해야 했던 날

## 진행 예정 내역
남은 서치헬프(평가클래스, 단위, 언어, 계정타입, 회계결정코드, 회계 계정 등)를 이어서 만들고, FI(02) 보고서의 첫 단계인 FI 계정 마스터(ZTB07SKA1) 관련 작업을 8/16부터 본격적으로 시작한다.
