# 개발일지 (2026-09-06)

## 어제까지 진행 상황
- 130, 103, 104까지 다 만들고 테스트하는 것과, 생성/수정/삭제를 RAP 단의 락·드래프트 기능을 적용해 완성하는 것이 남은 과제였음.

## 오늘 목표
- 130번 만든 다음에 102 & 130 테스트, 103, 104 만든 다음에 테스트.
- 생성, 수정, 삭제를 RAP 단의 락, 드래프트 기능을 적용하여 완성하는 게 목표.

## 진행 내용

### 100번 화면 디자인 변경 — ALV 그리드 타이틀 일부 삭제
100번(아이템), 101번(벤더 리스트), (200/300 결정 대기) PO 목록 ALV 상단의 그리드 타이틀을 삭제. 103번(옵션가)·104번(BOM 비교)은 유지.

관련 오브젝트: [`ZB07EKKO_F01`](../../src/06_purchase-order/prog/ZB07EKKO_F01.prog.abap)

```abap
*&---------------------------------------------------------------------*
*& Form set_layout (ALV 레이아웃 — pv_type별 분기)
*&---------------------------------------------------------------------*
FORM set_layout USING pv_type TYPE i
                CHANGING ps_layout TYPE lvc_s_layo.
  CLEAR ps_layout.
  ps_layout-zebra    = 'X'.
  ps_layout-sel_mode = 'A'.
  IF pv_type = 1.                        " 100번 아이템 ALV
*    ps_layout-grid_title = '구매오더 아이템'.
    IF gv_mode <> 'D'.
      ps_layout-sel_mode = 'B'.
    ENDIF.
  ELSEIF pv_type = 2.                    " 103번 옵션가(EINA/EINE 단가)
    ps_layout-grid_title = '구매정보레코드 기준 단가'.
  ELSEIF pv_type = 3.                    " (200/300 결정 대기) PO 목록
*    ps_layout-grid_title = '구매오더 목록'.
    ps_layout-sel_mode   = 'B'.
    ps_layout-cwidth_opt = 'X'.
  ELSEIF pv_type = 4.                    " 101번 벤더 리스트
*    ps_layout-grid_title = '공급업체 목록'.
    ps_layout-cwidth_opt = 'X'.
  ELSEIF pv_type = 5.                    " 104번 BOM 비교
    ps_layout-grid_title = '전사 BOM 구성 비교'.
    ps_layout-cwidth_opt = 'X'.
  ENDIF.
ENDFORM.
```

### 130번 로직 구성 — 핫스팟 이벤트(C01 신규) + 관련 주석 해제
101번 벤더 ALV에서 벤더(LIFNR/NAME1) 핫스팟 클릭 시 130번 팝업을 띄우는 이벤트 핸들러를 신규로 작성(`ZB07EKKO_C01` 신규 생성).

관련 오브젝트: [`ZB07EKKO_C01`](../../src/06_purchase-order/prog/ZB07EKKO_C01.prog.abap) · [`ZB07EKKO_O01`](../../src/06_purchase-order/prog/ZB07EKKO_O01.prog.abap) · [`ZB07EKKO_I01`](../../src/06_purchase-order/prog/ZB07EKKO_I01.prog.abap)

```abap
*&---------------------------------------------------------------------*
*& Include          ZB07EKKO_C01
*&---------------------------------------------------------------------*
CLASS lcl_event_handler DEFINITION.
  PUBLIC SECTION.
    CLASS-METHODS:
      "[101번] 벤더 ALV에서 핫스팟(LIFNR/NAME1) 클릭 시 130번 팝업 호출
      on_hotspot_click FOR EVENT hotspot_click
        OF cl_gui_alv_grid IMPORTING e_row_id e_column_id.
ENDCLASS.
CLASS lcl_event_handler IMPLEMENTATION.
  METHOD on_hotspot_click.
    " 1. 핫스팟 대상 컬럼인지 확인 — 공급업체번호/공급업체명 컬럼만 반응
    CHECK e_column_id-fieldname = 'LIFNR' OR e_column_id-fieldname = 'NAME1'.
    " 2. 클릭된 행의 벤더(LIFNR)로 상세 조회
    READ TABLE gt_vend INTO DATA(ls_selected_vend) INDEX e_row_id-index.
    IF sy-subrc = 0.
      " 3. 팝업용 조회(pv_pop = 'X') — gs_vend에 벤더 상세 채움
      PERFORM get_vendor_data USING 'X' ls_selected_vend-lifnr.
      " 4. gs_vend가 정상적으로 채워졌으면 130번 팝업 호출
      IF gs_vend-lifnr IS NOT INITIAL.
        CALL SCREEN '0130' STARTING AT 50 5
                           ENDING   AT 135 25.
      ENDIF.
      " 5. 팝업에서 SEL_VEND로 선택한 벤더가 방금 클릭한 벤더와 같으면(=방금 선택됨)
      "    I01의 WHEN OTHERS는 gs_head-lifnr <> gv_before_lifnr일 때만 도니까,
      "    102번 서브스크린 전환이 즉시 반영되도록 PAI를 한 번 강제로 더 돌림
      IF gs_head-lifnr = gs_vend-lifnr.
        cl_gui_cfw=>set_new_ok_code( EXPORTING new_code = 'ENTER' ).
      ENDIF.
    ENDIF.
  ENDMETHOD.
ENDCLASS.
```

101번 벤더 ALV(`INIT_ALV_0101`)에 주석 처리돼 있던 이벤트 핸들러 등록을 해제.

```abap
*&---------------------------------------------------------------------*
*& Module INIT_ALV_0101 OUTPUT (101번 벤더 리스트 ALV 초기화)
*&---------------------------------------------------------------------*
MODULE init_alv_0101 OUTPUT.
  PERFORM get_vendor_all_data.
  IF go_cont_vend IS INITIAL.
    PERFORM create_object USING 'VENDOR' '' CHANGING go_cont_vend go_alv_vend.
    PERFORM set_layout USING 4 CHANGING gs_layo_vend.
    PERFORM set_uifunc USING 2 CHANGING gt_uifunc_vend.
    PERFORM set_fcat_vend CHANGING gt_fcat_vend.
    SET HANDLER lcl_event_handler=>on_hotspot_click FOR go_alv_vend.
    PERFORM display_alv USING gs_layo_vend gt_uifunc_vend
                        CHANGING go_alv_vend gt_vend gt_fcat_vend.
  ELSE.
    go_alv_vend->refresh_table_display( ).
  ENDIF.
ENDMODULE.
```

I01의 `WHEN OTHERS`에서도 벤더 로직 호출 퍼폼문 주석을 해제.

```abap
	* .. 생략
 WHEN OTHERS.
      IF gs_head-lifnr IS NOT INITIAL.
        IF gs_head-lifnr <> gv_before_lifnr.
*          PERFORM get_header_data.
          PERFORM get_vendor_data USING '' gs_head-lifnr.
          IF gv_visible = 'X'.
*            PERFORM get_opti_data.
          ENDIF.
          gv_before_lifnr = gs_head-lifnr.
        ENDIF.
      ELSE.
*        PERFORM get_vendor_all_data.
        IF gv_visible = 'X'.
*          PERFORM get_opti_data.
        ENDIF.
*        PERFORM clear_header_data.
      ENDIF.
  ENDCASE.
ENDMODULE.
```

**요약 흐름**: 101 핫스팟 클릭 → C01이 `get_vendor_data('X', 클릭한 lifnr)`로 `gs_vend` 채우고 `CALL SCREEN 0130` → (기존 `status_0130` PBO는 PF-STATUS/TITLEBAR만 세팅하는 그대로 두면 됨, 데이터는 이미 채워져 있음) → 130에서 SEL_VEND 누르면 기존 로직대로 `gs_head-lifnr` 세팅 후 `LEAVE TO SCREEN 0` → C01이 방금 선택된 벤더와 같은지 확인해 `ENTER`를 강제로 흘려보냄 → I01의 `WHEN OTHERS`가 그 ENTER를 받아 `get_vendor_data('', lifnr)` 실행 → `gv_dynnr = '0102'`로 서브스크린 전환.

### 테스트 (1)

130번 ⇒ 기본은 잘 나오는 거 확인. 비주얼적으로 좀 수정이 필요함 + 아직 input 가능으로 되어있는 필드들 전부 input 불가로 해놓기(하지만 output only는 아님 — 텍스트 필드만). 계정 타입 텍스트 출력 안되고 있음.

102 외 ⇒ 102번도 잘 나옴. 130번에서 선택 눌렀을 때 헤더 정보까지 꽂히도록 100번 필드 관련 로직 추가해야할 듯 — 데이터에서는 `EKORG/EKGRP`가 `P02`로 되어있었는데, 실제 100번 화면에 꽂힌 건 기존 하드코딩(회사코드/구매조직/구매그룹 순서대로 K200, 1000, 001)된 값이었음. `GV_구매조직`, `GV_구매그룹` 변수의 값이 하드코딩 관련 값으로 되어 있어, P02가 꽂혀있어도 이 신규 데이터로 갱신이 안 됨.

#### 테스트를 통해 발견한 오류 해결 (1)

1. 계정 타입 텍스트 안나오는 문제

> **GLACT 도메인 확인 결과**: 지금 F01 코드가 존재하지도 않는 `'ZDB07GLACT'`를 도메인명으로 넘기고 있어서 `GET_DOMAIN_VALUES`가 값을 못 찾고 텍스트가 비는 거였습니다.
>
> 그리고 EKORG/EKGRP 헤더 반영 문제 — 1기 F01의 `get_header_data`/`get_vendor_data`를 다시 열어보니 정확히 이 상황을 위한 로직이 이미 있었어요(BP별 구매조건 조회 후 `gs_head-ekorg/ekgrp/bukrs`까지 덮어쓰고 텍스트 재조회). 2기는 벤더 마스터(`ZTB07LFA1`)에 EKORG/EKGRP가 이미 있고 별도 BUKRS 필드는 없으니, 그 구조에 맞게 이식하면 됩니다.

⇒ 응 그냥 어제 구매조직, 그룹 때랑 동일한 거였음.
⇒ 그래서 (1) EKORG, EKGRP 때처럼 Select문(CDS View)을 하거나, 아니면 (2) 그 CDS 뷰에서 쓰던 도메인 타입 `GLACCOUNT_TYPE` 자체를 쓰는 방법이 있는데 후자를 선택.

관련 오브젝트: [`ZB07EKKO_F01`](../../src/06_purchase-order/prog/ZB07EKKO_F01.prog.abap)

```abap
PERFORM get_domain_text USING 'GLACCOUNT_TYPE' gs_vend-glact CHANGING gv_glact.
```

✅ 해결
- GLACT(계정타입) 텍스트 미표시 문제 — 도메인명을 `'ZDB07GLACT'`(존재하지 않음)에서 `'GLACCOUNT_TYPE'`으로 수정.

🐛 문제 (미해결)
- 130번에서 벤더 선택 시 100번 헤더의 EKORG/EKGRP가 하드코딩 값(1000/001)에서 선택한 벤더의 실제 값(P02 등)으로 갱신되지 않는 문제 — 1기 로직 이식 방향만 확인했고, 오늘 코드 반영은 아직 안 됨.

### 시도 (Git) — abapGit 설치
자꾸 코드와 깃허브 사이의 텀이 존재해서 헷갈려, 이전 피오리 때처럼 GitHub에 바로 올라가도록 하고 싶었음.

- **호스트**: `bgissap1`(또는 IP `61.97.134.34`)
- **인스턴스 번호**: `00`(애플리케이션 서버 이름 `bgissap1_EFX_00`에서 `_00`이 인스턴스 번호)
- **SID**: EFX
- 포트 공식 적용: HTTPS = 443 + 00 = **44300**, HTTP = 80 + 00 = **8000**

STAD(Single Statistical Records)로 들어가면 누가/언제/어떤 트랜잭션으로/어떤 프로그램을 실행했는지가 개별 기록으로 남기 때문에, 남의 abapGit을 쓰지 않고 직접 프로그램을 만들기로 함.

`https://raw.githubusercontent.com/abapGit/build/main/zabapgit_standalone.prog.abap` — abapGit 공식 조직(`github.com/abapGit`)의 `build` 저장소에서 직접 받은 파일을 그대로 다운로드. `ZREPORT_B07` 생성 후, 소스코드 편집 모드에서 Utilities → More Utilities → Upload/Download → Upload로 저장한 파일을 업로드함.

※ `ZREPORT_B07`은 abapGit 공식 저장소의 외부 코드를 그대로 옮긴 설치용 프로그램이라 이번엔 `src/`에 별도 파일로 만들지 않고 이 devlog 기록으로만 남김.

## 오늘 배운 점 / 막힌 점
- EKORG/EKGRP처럼 어제와 같은 유형의 문제(도메인 Fixed Value 부재)가 GLACT에서도 반복됨 — 도메인 텍스트가 안 나오면 우선 해당 도메인명이 실제 존재하는지부터 의심할 것.
- 130번 선택 시 100번 헤더 갱신 로직은 1기(`get_header_data`/`get_vendor_data`)에 이미 있던 패턴을 2기 벤더 마스터(`ZTB07LFA1`) 구조에 맞게 이식하면 되는 방향까지만 확인, 아직 코드로 옮기지 못함.

## 내일 할 일
- 130번 화면 비주얼 수정 + input 가능 필드들 전부 input 불가(텍스트 필드만 output only 아님)로 정리.
- 130번에서 벤더 선택 시 100번 헤더(EKORG/EKGRP)가 하드코딩 값 대신 실제 선택값으로 갱신되도록 1기 로직 이식.
- GV_VEND_EKGRP/GV_VEND_EKORG 관련 지적사항(100/130 텍스트 변수 공유 문제) 정리.
